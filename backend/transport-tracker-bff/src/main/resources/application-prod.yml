app:
  transit-api:
    mta:
      api-key: ${MTA_API_KEY}
    transit-land:
      api-key: ${TRANSIT_LAND_API_KEY}
  cors:
    allowed-origins: ${CORS_ORIGINS}

logging:
  level:
    com.tracker: WARN
    root: ERROR
