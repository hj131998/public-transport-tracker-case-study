# Copy to .env.local for local development
# Vite exposes only variables prefixed with VITE_

# Backend API base URL — Vite dev server proxies /api to localhost:8080
# In production this is handled by nginx proxy_pass
VITE_API_BASE_URL=/api/v1
